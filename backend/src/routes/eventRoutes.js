import express from 'express';

const router = express.Router();

// TODO: import and wire controllers
// router.get('/', getEvents);
// router.post('/', protect, createEvent);

export default router;